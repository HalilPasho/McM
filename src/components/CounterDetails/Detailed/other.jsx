import React from "react";

const other = ({ other }) => {
  return (
    <div className="hg_desc">
      <span className="nr_ab">{other}</span>
      <p className="text_desc text_ab">Other</p>
    </div>
  );
};

export default other;
